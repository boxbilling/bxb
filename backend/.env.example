DEBUG=false
APP_NAME=bxb API
APP_DOMAIN=example.com
APP_DATA_PATH=/var/lib/app
APP_DATABASE_DSN=postgresql://user:pass@host:port/database
REDIS_URL=redis://localhost:6379
OPENROUTER_API_KEY=
SENTRY_DSN=

# Payment providers
stripe_api_key=
stripe_webhook_secret=
manual_webhook_secret=

# Webhook signing
webhook_secret=whsec_default_secret

# UCP (Universal Commerce Protocol)
ucp_base_url=
ucp_api_key=
ucp_webhook_secret=
ucp_merchant_id=

# GoCardless
gocardless_access_token=
gocardless_webhook_secret=
gocardless_environment=sandbox

# Adyen
adyen_api_key=
adyen_merchant_account=
adyen_webhook_hmac_key=
adyen_environment=test
adyen_live_url_prefix=

# SMTP email
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=billing@example.com
SMTP_FROM_NAME=Billing
SMTP_USE_TLS=true

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Portal
PORTAL_JWT_SECRET=portal-secret-change-me

# Rate limiting
RATE_LIMIT_EVENTS_PER_MINUTE=1000

# ClickHouse
CLICKHOUSE_URL=